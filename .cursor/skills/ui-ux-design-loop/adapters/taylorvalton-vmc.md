# taylorvalton / VMC adapter (live reference)

Canonical adapter: `ui-loop.config.json` at repository root.

This repository (`/home/ubuntu/agentic-swarm-ui-ux-design-loop`) is the operator's VMC codebase, and root `ui-loop.config.json` is the live reference adapter used by the resolver and gate scripts.

No duplicate adapter file is kept under `adapters/` for this repo by design, to avoid drift from the canonical root manifest.

## Why no additional taylorvalton repo entries here

Network access is firewalled for this phase, so other `github.com/taylorvalton/*` repositories are not available to clone on this box right now. Additional adapter entries should be added when those repos are locally available.
