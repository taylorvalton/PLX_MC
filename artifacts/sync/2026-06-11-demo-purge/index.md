# Bundle index — 2026-06-11-demo-purge

- `REPORT.md` — fixture + staging-mirror purge of all prototype/demo data
  (operator request): what was removed, the scoped DB deletes, post-purge
  verification, and the dev-site/folder-count caveats.

Related repo files: `src/lib/mc-data/data.ts`, `src/lib/mc-data/store.ts`,
`src/components/mc/` (chrome, task-detail, bucket-detail, sync-console,
agent-feed, traceability), `src/lib/sync/engine.ts`, `tests/`.
