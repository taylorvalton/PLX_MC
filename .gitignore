# Python (governance tooling)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# Playwright E2E (browser binaries are global; these are per-run outputs)
test-results/
playwright-report/
playwright/.cache/

# OS / editor noise
.DS_Store
Thumbs.db

# Local env — secrets come from AWS Secrets Manager, never files
.env
.env.*

# vercel deploy metadata
.vercel
.vercel

# Cursor skill mirrors (bootstrap copies from agentic-swarm; PLX adapters stay in git)
.cursor/skills/*
!.cursor/skills/project-hardener/
!.cursor/skills/project-orchestrator/
!.cursor/skills/ui-ux-design-loop/
.cursor/skills.backup-*/
.agents/

# Project Orchestrator scratch (specs, phase evidence) — promote to artifacts/ if kept
.orchestrator/
