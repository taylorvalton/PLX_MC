# Python (governance tooling)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# Playwright E2E (browser binaries are global; these are per-run outputs)
test-results/
playwright-report/
playwright/.cache/

# OS / editor noise
.DS_Store
Thumbs.db

# Local env — secrets come from AWS Secrets Manager, never files
.env
.env.*

# vercel deploy metadata
.vercel
.vercel

# Project-scoped skills for Cursor Cloud / picker (seeded from petralabx/skills).
# Source of truth: https://github.com/petralabx/skills — do not treat this tree as SoT.
# (Previously ignored all but three PLX adapters; that hid the pack from / .)
.agents/

# Project Orchestrator scratch (specs, phase evidence) — promote to artifacts/ if kept
.orchestrator/

# Vendored agentic-swarm checkout (cloned by .cursor/environment.json for swarm delegation)
.swarm/
.env*

# Session-brain offline queue (fail-open local capture; not evidence bundles)
artifacts/session-brain/
