# Governance tooling only — the product stack is Next.js + TypeScript.
# These power scripts/preflight.sh, the drift gate, the hygiene gate, and hooks.
pyyaml==6.0.3
ruff==0.15.16
pytest==9.0.3
pre-commit==4.6.0
